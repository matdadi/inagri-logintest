import mysql.connector
db = mysql.connector.connect(host='localhost',
                                         database='dadi-submission',
                                         user='root',
                                         password='')

